name: Release

on:
  push:
    branches:
      - master
      - 'alpha/**'

permissions:
  id-token: write
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '24'
          registry-url: "https://registry.npmjs.org"
          cache: 'npm'

      - run: npm ci

      - run: npm run build

      - run: npm run test

      - name: Read version
        id: readPackage
        run: |
          echo "name=$(node -p "require('./package.json').name")" >> "$GITHUB_OUTPUT"
          echo "version=$(node -p "require('./package.json').version")" >> "$GITHUB_OUTPUT"

      - name: Check if version exists
        id: checkVersion
        run: |
          if npm view "${{ steps.readPackage.outputs.name }}@${{ steps.readPackage.outputs.version }}" version >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "Version ${{ steps.readPackage.outputs.version }} already exists in registry."
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "Version ${{ steps.readPackage.outputs.version }} not found in registry; will publish."
          fi

      - run: npm publish
        if: steps.checkVersion.outputs.exists == 'false'

      - name: Github release
        uses: softprops/action-gh-release@v3
        if: steps.checkVersion.outputs.exists == 'false'
        with:
          name: Release v${{ steps.readPackage.outputs.version }}
          tag_name: v${{ steps.readPackage.outputs.version }}
          prerelease: ${{ contains(github.head_ref, 'alpha') }}
